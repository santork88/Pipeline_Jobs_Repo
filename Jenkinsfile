pipeline {
agent any
stages {
  stage('build') {
      // One or more stages need to be included within the stages block.
    }
  }

  stage('deploy') {
    stages {
      // One or more stages need to be included within the stages block.
    }
  }

  stage('test') {
    stages {
      // One or more stages need to be included within the stages block.
    }
  }

}
